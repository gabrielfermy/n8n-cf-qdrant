# ===================================================================
#     GENERAL SETTINGS (used by both Windows and Ubuntu setups)
# ===================================================================

# The top-level domain you own and have configured in Cloudflare
DOMAIN_NAME=your-domain.com

# Optional: The timezone for n8n scheduling (e.g., Cron nodes)
GENERIC_TIMEZONE=Asia/Jakarta

# ===================================================================
#     WINDOWS DOCKER-COMPOSE SETTINGS (in the root folder)
# ===================================================================

# Subdomain for the n8n UI and API (e.g., n8n.your-domain.com)
SUBDOMAIN=n8n

# Subdomain for all incoming webhooks (e.g., hooks.your-domain.com)
WEBHOOK_SUBDOMAIN=hooks

# Subdomain for the first WhatsApp instance (running on Windows)
WAHA_SUBDOMAIN=waha1

# Subdomain for securely connecting to your PostgreSQL database
POSTGRES_SUBDOMAIN=postgres

# The Cloudflare Tunnel Token for the Windows machine.
# Get this by running: cloudflared tunnel create n8n-windows
TUNNEL_TOKEN=

# ===================================================================
#     UBUNTU DOCKER-COMPOSE SETTINGS (in the /ubuntu folder)
# ===================================================================

# Subdomain for the second WhatsApp instance (running on Ubuntu)
WAHA2_SUBDOMAIN=waha2

# The Cloudflare Tunnel Token for the Ubuntu machine.
# Get this by running: cloudflared tunnel create waha-ubuntu
TUNNEL_TOKEN_WAHA2=
